# Optional dependencies for the real-data run (run_full.py export helpers).
# Not needed to run the app or the offline demo (build_demo_db.py).
#   pip install -r requirements-etl.txt
nba_api==1.5.2          # NBA + (indirectly) the stats.* request shape
nfl_data_py==0.3.3      # NFL / nflverse
# NHL (api-web.nhle.com) and WNBA (stats.wnba.com) use `requests`, already
# in requirements.txt. NCAAF uses the CollegeFootballData API (urllib).
